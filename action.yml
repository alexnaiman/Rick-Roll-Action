name: "Rick Roll Action"
description: "Never Gonna Give You Up - Action that will turn any package.json into a Rick Roll"
inputs:
  github-token:
    description: "Github token to have access to the repo"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
